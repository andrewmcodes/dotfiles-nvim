-- Hotwired Stimulus support is fully provided by:
--   * stimulus_ls  (enabled in lsp.lua) for completion/hover/definition
--   * treesitter + filetype detection (JS/JSX + ERB) for editing ergonomics
-- No dedicated Neovim plugin is required, so this file intentionally returns an
-- empty spec list (a valid lazy.nvim import).
return {}
